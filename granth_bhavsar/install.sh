#!/usr/bin/env bash

mkdir -p $HOME/.config/wal/templates # makes sure required directories exist
cp ./template/custom_palettes.toml $HOME/.config/wal/templates/ #copies template to required directory
echo "Install Completed!" 

